Commands that we saw today:

1. git init        _# for intitialization_
2. git config --global user.name "username"
3. git config --global user.email "user.email@gmail.com"
4. git clone "repo-link"
5. cd repo-link     _# Navigate to the repo_
6. git add <filename>
7. git status 
8. git commit -m "Your Comments"
9. git push
